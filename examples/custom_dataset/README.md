# Custom dataset examples

Each sample is a directory containing `input.png` and a non-empty UTF-8
`prompt.txt`. An optional `metadata.json` may override generation options
documented in the [custom dataset workflow](../../docs/workflows/custom-dataset.md).

The included samples are small workflow examples, not benchmark data.
